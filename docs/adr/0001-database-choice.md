# ADR 0001: Use a Relational Database for Data Storage

## Status
Accepted

## Context
We need to store structured data with relationships between entities.

## Decision
We will use a relational database instead of a NoSQL store.

## Consequences
Provides strong consistency and easy querying, but requires
defining a schema up front.
